[project]
name = "{{cookiecutter.project_slug}}"
version = "0.1.0"
description = "{{cookiecutter.description}}"
requires-python = ">={{cookiecutter.python_min}}"
dependencies = [
  "slmlab",
  "transformers>=4.44",
  "datasets>=2.21",
  "peft>=0.11",
  "evaluate>=0.4",
  "typer[all]",
  "lxml",
]

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["{{cookiecutter.project_slug}}"]

[tool.uv.sources]
slmlab = { git = "{{cookiecutter.core_repo_url}}" }